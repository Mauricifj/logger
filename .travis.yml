language: android
dist: trusty
android:
  components:
    # The BuildTools version used by your project
    - build-tools-29.0.2

    # The SDK version used to compile your project
    - android-29

# deploy section
deploy:
  provider: script
  script: ./gradlew clean build assemble bintrayUpload
  on:
    branch: master
